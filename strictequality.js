// = assignment operator, == check if two values are equal, === check if two values and there data types are equal, != inequality operator, !== strict equality operator


const pi = 3.14;

if (pi !== "3.14") {
    console.log("that is pi");
} else {
    console.log("that is not pi");
}

// const pi = 3.14;

// if (pi != "3.14") {
//     console.log("that is pi");
// } else {
//     console.log("that is not pi");
// }